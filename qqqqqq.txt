#!/bin/bash

# 设置 GOOGLE_APPLICATION_CREDENTIALS 环境变量
export GOOGLE_APPLICATION_CREDENTIALS="/path/to/your-service-account-key.json"

# 运行 Java 程序
java -cp ".:path/to/google-cloud-storage.jar" UploadObject \
    your-project-id \
    your-bucket-name \
    your-object-name \
    path/to/your/file





#!/bin/bash

# 设置Google Cloud认证文件路径
export GOOGLE_APPLICATION_CREDENTIALS="/path/to/my-service-account-key.json"

# 定义Java程序参数
PROJECT_ID="my-gcp-project"
BUCKET_NAME="my-bucket"
OBJECT_NAME="my-object"
FILE_PATH="/path/to/myfile.txt"

# 调用Java程序并传递参数
java -cp .:path/to/google-cloud-storage.jar Main $PROJECT_ID $BUCKET_NAME $OBJECT_NAME $FILE_PATH

# 检查Java程序是否成功执行
if [ $? -eq 0 ]; then
  echo "File uploaded successfully!"
else
  echo "Failed to upload file."
fi


================================


#!/bin/bash

# 定义参数
PROJECT_ID="my-gcp-project"       # 替换为你的GCP项目ID
BUCKET_NAME="my-bucket"           # 替换为你的GCS存储桶名称
OBJECT_NAME="my-object"           # 替换为你要上传的对象名称
FILE_PATH="/path/to/myfile.txt"   # 替换为你要上传的文件路径

# 调用Java程序并传递参数
java -cp .:path/to/google-cloud-storage.jar Main $PROJECT_ID $BUCKET_NAME $OBJECT_NAME $FILE_PATH

# 检查Java程序是否成功执行
if [ $? -eq 0 ]; then
  echo "File uploaded successfully!"
else
  echo "Failed to upload file."
fi
